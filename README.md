# llm-gateway

An OpenAI-compatible LLM gateway written in Go — reverse proxy handling auth, routing, rate limiting, caching, and retries/failover in front of local and hosted LLM providers.

🚧 Work in progress — currently implementing core routing and provider integration.
